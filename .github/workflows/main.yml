name: CI - Flask API (Docker)

on:
  push:
    branches: ["main"]
  pull_request:
    branches: ["main"]

jobs:
  build-and-test-docker:
    runs-on: ubuntu-latest
    steps:
      # 1️⃣ Checkout do código
      - name: Checkout do código
        uses: actions/checkout@v4

      # 2️⃣ Configurar Docker Buildx (para builds multi-plataforma se quiser)
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v2

      # 3️⃣ Build da imagem Docker
      - name: Build Docker image
        run: docker build -t flask-api .

      # 4️⃣ Rodar container e executar os testes
      - name: Run container and execute tests
        run: |
          docker run --name flask-test -d -p 1313:1313 flask-api
          sleep 5  # espera a API iniciar
          docker exec flask-test python -m unittest discover -s . -p "test_*.py"
          docker stop flask-test
          docker rm flask-test

  deploy:
    runs-on: ubuntu-latest
    needs: build-and-test-docker
    steps:
      # 1️⃣ Checkout do código
      - name: Checkout do código
        uses: actions/checkout@v4

      # 2️⃣ Instalar Vercel CLI
      - name: Install Vercel CLI
        run: npm install -g vercel

      # 3️⃣ Deploy para Vercel (apenas se os testes passarem)
      - name: Deploy to Vercel
        env:
          VERCEL_TOKEN: ${{ secrets.VERCEL_TOKEN }}
          VERCEL_ORG_ID: ${{ secrets.VERCEL_ORG_ID }}
          VERCEL_PROJECT_ID: ${{ secrets.VERCEL_PROJECT_ID }}
        run: |
          vercel --token=$VERCEL_TOKEN --prod --confirm
